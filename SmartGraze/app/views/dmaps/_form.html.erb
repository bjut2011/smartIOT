<%= form_for(@dmap) do |f| %>
  <% if @dmap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dmap.errors.count, "error") %> prohibited this dmap from being saved:</h2>

      <ul>
      <% @dmap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
