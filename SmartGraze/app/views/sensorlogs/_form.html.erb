<%= form_for(@sensorlog) do |f| %>
  <% if @sensorlog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sensorlog.errors.count, "error") %> prohibited this sensorlog from being saved:</h2>

      <ul>
      <% @sensorlog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.number_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
